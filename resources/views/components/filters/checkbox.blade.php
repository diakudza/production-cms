<div><input type="checkbox"
            placeholder="{{ $placeholder }}"
            name="{{ $name }}"
            class="checkbox checkbox-bordered"
            @checked(request($name))
            value="on"
    />
    <p>c фото</p></div>